[Unit]
Description=A high performance web server and a reverse proxy server(SV)

[Service]
Type=simple
PIDFile=/var/run/nginx.pid
ExecStart=/usr/bin/sv start nginx
ExecStop=/usr/bin/sv stop nginx
ExecReload=/usr/bin/sv reload nginx
PrivateTmp=true

[Install]
WantedBy=multi-user.target
